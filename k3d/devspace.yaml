version: v1beta11

vars:
- name: K3D_CLUSTER_NAME
  question: cluster name for k3d
  default: "devspace"

commands:
- name: reset
  command: "k3d cluster delete ${K3D_CLUSTER_NAME} && k3d cluster create ${K3D_CLUSTER_NAME}"
