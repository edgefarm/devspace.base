version: v1beta11

dependencies:
- name: k3d
  source:
    path: ../k3d
- name: sealed-secrets
  source:
    path: ../sealed-secrets

profiles:
- name: init
  patches:
  - op: add
    path: dependencies.name=sealed-secrets.profile
    value: "init"
